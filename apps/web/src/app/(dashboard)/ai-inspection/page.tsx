import { ComingSoon } from "@/components/coming-soon";

export default function AiInspectionPage() {
  return (
    <ComingSoon
      title="AI Food Quality Inspection"
      description="Inspections run automatically when a task is finished with a photo proof — view results inside each task once scored."
    />
  );
}
