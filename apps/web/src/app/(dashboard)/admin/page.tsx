import { ComingSoon } from "@/components/coming-soon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin Panel"
      description="Central control for recipes, departments, employees, AI settings, notifications, users, permissions, and audit logs."
    />
  );
}
