import { ComingSoon } from "@/components/coming-soon";

export default function FaceRecognitionPage() {
  return (
    <ComingSoon
      title="Face Recognition"
      description="Attendance kiosk camera enrollment and recognition — backed by a swappable provider interface, currently mocked."
    />
  );
}
